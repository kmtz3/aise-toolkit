# Team Roster

> The AISEs on this leader's direct team. Populated by /assistant-setup via auto-discovery from Planhat; update via /assistant-setup --update or edit directly. Re-run /assistant-setup --update to re-validate Planhat user IDs after org changes.

## Team Members

| Name | Email | Planhat User ID | Active |
|---|---|---|---|
| `<TBD>` | `<TBD>` | `<TBD>` | Yes |

## How this is used

- **Planhat query scoping:** team-wide reports filter by any Planhat user `_id` in the Active rows above (`ownerId` on Tasks, `users` on Conversations).
- **Gong host filtering:** session searches filter by host email matching any Active email above.
- **Per-AISE reports** (`/report --aise <name>`): filter by that AISE's UUID only.
- **Scorecard roll-ups:** iterate over Active rows and aggregate session data per AISE.
