# Cross-Customer Observations — moved

This file has moved to the per-user data directory so observations stay private to the installer and survive plugin updates.

**Location:** `<PLUGIN_DATA_DIR>/about/tracker-memory.md`
where `<PLUGIN_DATA_DIR>` = the path in `~/.claude/aise-assistant.datadir`.

Run `/assistant-setup` to seed the file if it does not exist yet.
